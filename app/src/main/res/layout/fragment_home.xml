<?xml version="1.0" encoding="utf-8"?>
<!-- Home screen: the sky-blue header (app title + logout) and the two feed tabs
     (Discovered / Finding). This is the content that used to live in MainActivity. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/off_white_surface">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/sky_blue"
        android:elevation="0dp"
        app:liftOnScroll="true">

        <!-- Header row: title on the left, logout icon on the right. -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="12dp"
            android:paddingTop="28dp"
            android:paddingBottom="16dp">

            <TextView
                android:id="@+id/textAppTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:text="@string/app_name"
                android:textColor="@color/white"
                android:textSize="40sp"
                android:letterSpacing="0.01"
                app:fontFamily="@font/chango_regular" />

            <TextView
                android:id="@+id/textUserGreeting"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/textAppTitle"
                android:layout_alignStart="@id/textAppTitle"
                android:layout_marginTop="2dp"
                android:textColor="@color/sky_blue_deep_text"
                android:textSize="13sp"
                android:fontFamily="@font/inter_medium"
                tools:text="Hi, Ray!" />

            <ImageButton
                android:id="@+id/buttonLogout"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_logout"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="centerInside"
                android:contentDescription="@string/logout_description" />

            <ImageButton
                android:id="@+id/buttonSearch"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_toStartOf="@id/buttonLogout"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_search"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="centerInside"
                android:contentDescription="@string/search_icon_description" />
        </RelativeLayout>

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/sky_blue"
            app:tabIndicatorColor="@color/white"
            app:tabIndicatorHeight="3dp"
            app:tabIndicatorFullWidth="false"
            app:tabSelectedTextColor="@color/white"
            app:tabTextColor="@color/sky_blue_deep_text"
            app:tabTextAppearance="@style/TabTextAppearance"
            app:tabRippleColor="@android:color/transparent" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>