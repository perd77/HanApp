<?xml version="1.0" encoding="utf-8"?>
<!-- App shell: a fragment container fills the screen, with a BottomAppBar pinned
     to the bottom and a FAB docked into its center notch. The visible tabs and
     header now live inside HomeFragment, not here. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/off_white_surface">

    <!-- Swappable content area. Home or Notifications fragments are shown here.
         Bottom padding keeps content clear of the BottomAppBar. -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/navHostContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="80dp" />

    <!-- Bottom bar with a cradle (notch) cut out for the FAB.
         fabCradleMargin / cornerRadius shape how the notch hugs the FAB. -->
    <!-- Bottom bar with a cradle (notch) for the FAB, centered. -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="80dp"
        android:layout_gravity="bottom"
        android:backgroundTint="@color/bottom_bar_black"
        app:fabAlignmentMode="center"
        app:fabCradleMargin="10dp"
        app:fabCradleRoundedCornerRadius="16dp"
        app:fabCradleVerticalOffset="6dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="50dp"
            android:paddingEnd="50dp">

            <ImageButton
                android:id="@+id/navHome"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:src="@drawable/ic_home"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="centerInside"
                android:contentDescription="@string/nav_home"
                app:tint="@color/bottom_bar_icon" />

            <Space
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <ImageButton
                android:id="@+id/navNotifications"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:src="@drawable/ic_notifications"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="centerInside"
                android:contentDescription="@string/nav_notifications"
                app:tint="@color/bottom_bar_icon" />

        </LinearLayout>

    </com.google.android.material.bottomappbar.BottomAppBar>

    <!-- Docked "+" button. layout_anchor ties it to the bar; with
         fabAlignmentMode="center" above, it sits in a center notch. -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add"
        android:contentDescription="@string/fab_add_description"
        app:backgroundTint="@color/sky_blue"
        app:layout_anchor="@id/bottomAppBar" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>